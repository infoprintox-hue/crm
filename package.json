{
  "name": "visitinglink-business-os-next",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "check:connections": "node --env-file=.env.local scripts/check-connections.mjs",
    "check:storage": "node --env-file=.env.local scripts/check-storage.mjs",
    "seed:admin": "node --env-file=.env.local scripts/seed-admin.mjs"
  },
  "dependencies": {
    "cloudinary": "^2.7.0",
    "mongodb": "^6.18.0",
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
